# 複製此檔為 .env.local 覆寫預設環境變數（.env.local 不會進 Git）

# 是否啟用 mock server（dev 預設 true）
VITE_USE_MOCK=true

# 前端 axios 的 baseURL（預設 /ECAPI）
# VITE_API_BASE_URL=/ECAPI

# 當 VITE_USE_MOCK=false 時，Vite proxy 轉發 /ECAPI/* 到這個 target
VITE_API_TARGET=http://localhost:5000
# 正式環境可改：https://webris.ebmtech.com
